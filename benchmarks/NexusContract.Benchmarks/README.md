NexusContract BDN benchmarks

Run:

```powershell
cd benchmarks\NexusContract.Benchmarks
dotnet run -c Release
```

Results will be generated by BenchmarkDotNet under the default artifacts folder; consider copying `BenchmarkDotNet.Artifacts/results` to `benchmarks/results` after run.
